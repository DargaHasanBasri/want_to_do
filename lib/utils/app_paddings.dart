import 'package:flutter/material.dart';

final class AppPaddings {
  AppPaddings._();

  static const pagePaddingAll = EdgeInsets.all(24);
  static const pagePaddingHorizontal = EdgeInsets.symmetric(horizontal: 24);
}
